<Window x:Class="VisualHFT.TriggerEngine.View.TriggerSettingsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:VisualHFT.TriggerEngine.View"
          TextElement.Foreground="{DynamicResource MaterialDesignBody}"
  TextElement.FontWeight="Regular"
  TextElement.FontSize="15"
  TextOptions.TextFormattingMode="Ideal" 
  TextOptions.TextRenderingMode="Auto"      
        Background="{DynamicResource MaterialDesignPaper}"
        mc:Ignorable="d"
        Title="TriggerSettingsView" Height="450" Width="800">
    <Grid x:Name="MainGrid">

        <TabControl VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
            <TabItem Header="All Rules">
                <ScrollViewer>

                    <ListView ItemsSource="{Binding}">
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Header="Rule Name" DisplayMemberBinding="{Binding Name}" />
                                <GridViewColumn Header="Total Conditions">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding Condition.Count}" />
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Header="Total Actions">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding Actions.Count}" />
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Header="Actions">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock>
                    <Hyperlink Click="rule_Click"  x:Name="rule"
                               Command="{Binding DataContext.MethodCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                               CommandParameter="{Binding}"> Update
                    </Hyperlink>
                </TextBlock>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn> 
                            
                        </GridView>
                        </ListView.View>
                    </ListView>

                </ScrollViewer>
            </TabItem>
        </TabControl>
        
        <Button VerticalAlignment="Bottom" x:Name="NewRule" Click="NewRule_Click" HorizontalAlignment="Left">Add New Rule</Button>
    </Grid>
</Window>
